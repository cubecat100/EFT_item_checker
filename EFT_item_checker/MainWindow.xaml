<Window x:Class="EFT_item_checker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EFT_item_checker"
        mc:Ignorable="d"
        
        Closed="Window_Closed"
        Title="MainWindow" Height="450" MinHeight="450" Width="800" MinWidth="800">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="6*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="White" Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="55"/>
                        <RowDefinition Height="30*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="퀘스트/모듈 목록" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    
                    <Grid Grid.Row="1" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Center" IsEnabled="{Binding CheckBoxEnabled, Mode=TwoWay}">
                            <CheckBox Content="퀘스트" IsChecked="{Binding IsQuestsVisible}"/>
                        </TextBlock>
                        <TextBlock Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" VerticalAlignment="Center" IsEnabled="{Binding CheckBoxEnabled, Mode=TwoWay}">
                            <CheckBox Content="은신처 모듈" IsChecked="{Binding IsStationsVisible}"/>
                        </TextBlock>

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" IsEnabled="{Binding CheckBoxEnabled, Mode=TwoWay}">
                            <CheckBox Content="필수" IsChecked="{Binding IsKappaQuestsVisible}"/>
                        </TextBlock>
                        <TextBlock Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" IsEnabled="{Binding CheckBoxEnabled, Mode=TwoWay}">
                            <CheckBox Content="선택" IsChecked="{Binding IsSelectableQuestsVisible}"/>
                        </TextBlock>

                        <Button Content="Reset" Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2" Margin="2" Command="{Binding ResetCommand}"/>

                    </Grid>

                    <Grid Grid.Row="2">
                        <ListBox ItemsSource="{Binding VisibleTaskList}" BorderBrush="Black" BorderThickness="1"
                                 SelectedItem="{Binding SelectedTask, Mode=TwoWay}" SelectionMode="Single">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVisibility}" Value="Collapsed">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border HorizontalAlignment="Stretch" Visibility="{Binding IsVisibility}">
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                      Command="{Binding DataContext.ToggleSelectCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                                      CommandParameter="{Binding}"/>
                                            
                                            <TextBlock Text="{Binding Name}" Margin="5,0,0,0" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                    </Grid>

                </Grid>
                
            </Grid>

            <Grid Grid.Column="1" Background="White" Margin="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <TextBlock Text="아이템 목록" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>

                <Grid Grid.Row="1">
                    <ScrollViewer VerticalScrollBarVisibility="Visible">
                        <ItemsControl ItemsSource="{Binding FilteredItems}" >

                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- 아이콘 크기 설정 : 동적으로 변경할수도 -->
                                    <!-- 크기 변경시 폰트 크기 재설정할수도 -->
                                    <Grid Width="80" Height="80" Margin="5">

                                        <!-- 아이템 아이콘 -->
                                        <Image Source="{Binding Item.IconPath}" Stretch="Fill"/>

                                        <!-- 아이템 이름 표시 -->
                                        <Border Background="#80000000" CornerRadius="0,0,5,0" Padding="5,2" HorizontalAlignment="Left" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Item.Name}" Foreground="White" FontWeight="Bold"/>
                                        </Border>

                                        <!-- 아이템 수량 표시 -->
                                        <Border Background="#80000000" CornerRadius="5,0,0,0" Padding="5,2" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                                            <TextBlock Text="{Binding Quantity}" Foreground="White" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                        </Border>
                                    </Grid>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>

                    </ScrollViewer>
                </Grid>

            </Grid>
        </Grid>

        <Grid Grid.Row="1" Background="White" Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding Description}" VerticalAlignment="Center" Margin="15,5,15,5"/>

            <Button Grid.Column="1" Content="Go To Wiki"
                Command="{Binding OpenLinkCommand}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
    </Grid>
</Window>
